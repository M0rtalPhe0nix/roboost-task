# Roll out injection detection in monitor mode

Injection Signals are logged and flagged without automatically changing a listing's publication state until Arabic-English detector performance has been measured. A detected injection is an integrity concern rather than proof of a prohibited item, so enforcement requires an evidence-based threshold and false-positive review.
