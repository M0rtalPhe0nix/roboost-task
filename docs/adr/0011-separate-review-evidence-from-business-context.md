# Separate review evidence from business context

The AI Summary treats public reviews and the client-supplied Business Context Profile as distinct inputs. In Review-Only Mode it can surface evidence-bound Observations and generic testable responses, but cannot invent ownership, constraints, or feasibility; context-aware plans are enriched only from versioned client information.
